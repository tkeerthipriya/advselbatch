package Part1;

import java.time.Duration;
import java.util.Set;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.interactions.Actions;

public class createContactEndToEndTesting {
public static void main(String[] args) {
		// TODO Auto-generated method stub
		//WebDriver driver = new EdgeDriver() ;
		/*driver.manage().window().maximize();
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(20));
		//login Action
		driver.get("http://49.249.28.218:8098/");
		driver.findElement(By.id("username")).sendKeys("rmgyantra");
		driver.findElement(By.name("password")).sendKeys("rmgy@9999");
		driver.findElement(By.xpath("//button[@type='submit']")).click();
		//create contact
		driver.findElement(By.linkText("Contacts")).click();
		driver.findElement(By.xpath("//span[text()='Create Contact']")).click();
		driver.findElement(By.name("organizationName")).sendKeys("CPF india pvt ltd");
		driver.findElement(By.name("title")).sendKeys("Customer");
		driver.findElement(By.name("department")).sendKeys("HR");
		driver.findElement(By.name("officePhone")).sendKeys("8134256679");
		driver.findElement(By.name("contactName")).sendKeys("Mr.Surendra");
		driver.findElement(By.name("mobile")).sendKeys("9640744279");
		driver.findElement(By.name("email")).sendKeys("Keerthi24567@gmail.com");*/
		
	/*	WebElement mousehaver = driver.findElement(By.xpath("//*[name()='svg' and @data-icon='user']"));
		mousehaver.click();
	String parent = driver.getWindowHandle();
	Set<String> child = driver.getWindowHandles();
	child.remove(parent);
		
		for(String ele:child) {
		driver.switchTo().window(ele);
		WebElement select = driver.findElement(By.xpath("//button[contains(@onclick,'CAM00001')]"));
	select.click();}*/
	
		
	/*WebElement plus = driver.findElement(By.xpath("//*[name()='svg' and @data-icon='plus']"));
	plus.click();
	WebElement select = driver.findElement(By.xpath("//button[contains( @onclick,'CAM00001')]"));
	Actions act = new Actions(driver);
	act.moveToElement(select).click();*/
	
	
	
	

	}

}
